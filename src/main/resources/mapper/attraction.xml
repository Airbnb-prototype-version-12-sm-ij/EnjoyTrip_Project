<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip.dao.AttractionMapper">

    <select id="loadAttraction" resultType="com.ssafy.trip.model.AttractionDto">
        SELECT *
        FROM attraction_info ai
                 INNER JOIN attraction_description ades ON ai.content_id = ades.content_id
        WHERE content_type_id = #{type_code}
          AND sido_code = #{sido_code}
          AND title LIKE CONCAT('%', #{title}, '%')
    </select>

    <select id="pickAttraction" resultType="com.ssafy.trip.model.AttractionDto">
        SELECT *
        FROM attraction_info ai
                 INNER JOIN attraction_description ades ON ai.content_id = ades.content_id
        WHERE ai.content_id = #{content_id}
    </select>

    <select id="getGugun" resultType="com.ssafy.trip.model.AttractionDto">
        SELECT *
        FROM gugun
        WHERE sido_code = #{sido}
    </select>

</mapper>